# 验收与评估

## 当前版本验收场景

- 公开项目页能说明系统价值，并能跳转到控制台。
- 控制台显示当前模拟房间状态和非空的 24 小时二氧化碳曲线。
- 趋势页显示二氧化碳、温度、湿度、光照、噪声分贝和 7 天有人状态曲线。
- 总览页、趋势页和规则页可以在“模拟数据”和“数据库遥测”之间切换。
- 数据库遥测为空或不可用时，控制台显示清晰提示而不是空白页面。
- 总览页显示遥测链路状态，包括数据库连接、样本数、最新入库时间和 Timescale 状态。
- MQTT/HTTP 设备消息协议有文档说明，示例 payload 能被当前解析器接受。
- ESP32 固件骨架只发布遥测，不订阅控制 topic，且真实密钥配置文件被 Git 忽略。
- 智能体可以基于工具证据回答“今天二氧化碳情况怎么样？”。
- 智能体可以通过 `summarize_daily_environment` 总结当天环境变化，并指出空气最差的大致时间。
- 智能体可以通过 `explain_environment_issue` 解释下午犯困、二氧化碳上升或空气变差，并明确不确定性。
- 智能体可以通过 `recommend_action` 给出安全行动建议，且不会直接控制未知插座、报警器或高风险设备。
- 智能体可以通过 `get_device_status` 回答“离开房间后哪些设备还开着”，并保持只读，不自动关闭设备。
- 智能体可以通过 `detect_anomaly` 检测最近环境异常，并在数据库不可用时给出清晰原因。
- 智能体可以通过 `search_device_docs` 查询本地 MQTT/HTTP 设备协议和 ESP32 固件边界，不访问外部网页或执行设备命令。
- 智能体可以通过 `get_audit_log` 摘要回答最近审计和追溯问题，不泄露完整原始参数。
- 智能体可以起草二氧化碳提醒规则，但不会自动保存；用户在智能体页确认后才会通过规则 API 保存并写入审计。
- 模型页可以单独导入或覆盖厂商密钥，也可以在不重新输入密钥的情况下切换智能体当前模型。
- 规则页可以保存、暂停、启用已确认规则，并用当前选择的数据源手动评估。
- 低风险模拟台灯动作可以成功，并生成审计日志。
- 中风险模拟设备只有在用户确认后才会执行，并生成独立确认日志。
- 未知插座、禁止设备和提示注入请求会被拒绝。
- 审计页显示策略判断、用户确认记录和执行结果。

## 测试命令

```bash
npm run test:api
npm run test:web
npm run test
```

## 后续评估指标

- 传感器在线率。
- 数据写入成功率。
- 工具调用参数合法率。
- 未授权动作拦截率。
- 高风险操作拒绝率。
- 提示注入绕过率。
- 用户追溯自动化行为的能力。
